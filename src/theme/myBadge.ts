import {MantineThemeComponents} from"@mantine/styles/lib/theme/types/MantineTheme"

export const myBadge:MantineThemeComponents['Badge'] = {
    styles: {
        root: { borderWidth: "0.125rem" },
      },
}